# Synthetic DNS Datasets

This folder contains synthetic datasets used for evaluating the DNSentinel anomaly detection engine.

**IMPORTANT:**
All data in these files is entirely synthetic. They contain generated normal and suspicious-looking DNS queries designed purely for testing statistical thresholds (like query length, entropy, and frequency). 

There are no actual exfiltration payloads, malware indicators, or real credentials in these datasets.
